# SoccerMatchBetting 智能体工作规范

## 文件安全规则

禁止批量删除文件或者目录。

不要使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

需要删除文件时，只能一次删除一个明确路径的文件。

正确实例：

```powershell
Remove-Item "C:\path\to\file.txt"
```

如果需要批量删除文件，应停止操作，并向用户请求。

## 项目定位

本项目用于 2026 世界杯赛前资料整理、盘口快照记录、娱乐比分预测和赛后复盘。

智能体不得把预测包装成确定性结论，不得输出下注金额，不得给出稳赚判断，不得诱导投注。所有预测只作为娱乐和复盘参考。

## 默认语言

所有正式文档、比赛记录、预测输出和复盘记录默认使用中文。

球队英文名可以保留在文件名、来源链接、盘口原文和赛事官方名称中。

## 每次查询前必须执行的更新

当用户询问最近比赛、某场比赛、今日比赛或预测比分时，智能体必须先更新资料，再输出结论。

更新顺序：

1. 联网核对最新赛程，优先使用 FIFA 官方赛程和比分页面。
2. 检查公开赛程页面做交叉验证。
3. 读取本项目已有比赛文件，确认是否已有历史预测。
4. 使用用户提供的盘口文本或截图作为最高优先级盘口来源。
5. 如果用户未提供盘口，再联网查询公开盘口或赔率页面。
6. 更新比赛文件中的 `last_updated`、资料来源、盘口快照和赛前情报。
7. 如果比赛已经结束，先补充实际比分和赛后复盘，再继续处理新的预测。

## 盘口记录范围

每场比赛只固定记录以下三类盘口：

- 胜平负
- 亚洲让球
- 大小球

如果三类盘口不完整，必须在比赛文件中标注：

- `盘口状态：完整`
- `盘口状态：部分缺失`
- `盘口状态：缺失`

盘口不完整时仍可预测，但必须降低信心等级，并在风险提示中说明缺失项。

## 数据优先级

数据可信度优先级：

1. 官方赛程和官方比分。
2. 用户提供的盘口文本或截图。
3. 主流体育媒体赛程、球队新闻和伤停信息。
4. 公开赔率或盘口页面。
5. 历史文件中的旧记录。

如果来源冲突：

- 赛程和实际比分以官方来源为准。
- 盘口以用户提供的最新快照为准。
- 情报冲突时必须列出冲突点，不得强行合并成确定结论。

## 预测输出格式

每次预测输出必须包含：

1. 最近比赛列表。
2. 每场基础信息：时间、地点、赛事阶段、对阵队伍。
3. 盘口摘要：胜平负、亚洲让球、大小球。
4. 赛前情报摘要。
5. 两组推荐比分：
   - 稳健比分：贴近基本面和盘口共识。
   - 分歧比分：考虑冷门、平局、小胜、大球或小球偏移。
6. 信心等级：高 / 中 / 低。
7. 风险提示：冷门点、盘口异常、伤停不确定、资料缺失。
8. 文件更新说明：更新了哪些比赛文件或复盘文件。

## 预测模型

默认使用混合评分型策略。

每场比赛需要评估：

- 实力差
- 近期状态
- 阵容完整度
- 战术克制
- 盘口共识
- 进球倾向
- 冷门风险

不能只根据单一赔率或单一排名直接给比分。

## 赛后复盘

比赛结束后，后续查询必须更新：

- 实际比分
- 稳健比分是否命中
- 分歧比分是否命中
- 胜平负方向是否命中
- 大小球方向是否命中
- 让球方向是否命中
- 偏差原因

复盘重点是记录为什么偏离，而不是只记录输赢。

## 文件归档规则

比赛文件必须按赛事、日期、场次归档。

路径格式：

```text
tournaments/2026-world-cup/matches/YYYY-MM-DD/NNN-team-a-vs-team-b.md
```

每日汇总文件：

```text
tournaments/2026-world-cup/daily/YYYY-MM-DD.md
```

赛后复盘文件：

```text
tournaments/2026-world-cup/reviews/YYYY-MM-DD-review.md
```

资料源、预测方法和复盘规则分别维护在：

```text
data_sources/sources.md
methodology/prediction-model.md
methodology/review-rules.md
```

## 更新时间规则

每次修改比赛文件时，必须更新 `last_updated`。

时间默认使用北京时间，格式示例：

```text
2026-06-11T12:30:00+08:00
```

如果资料来自网页，必须记录查询时间和来源链接。
